//函数_作业讲解(下)

//return不能同时返回两个数
// arr[0]——> *(arr+0)
// arr[1]——> *(arr+1)

//1.实现一个函数，判断一个数是不是素数；利用函数打印100~200之间的素数
//2.实现函数判断year是不是闰年


///作业1：实现一个函数，判断一个数是不是素数；利用函数打印100~200之间的素数
/*
#include <stdio.h>

int is_prime(int n)
{
    int i = 0;
    for (i = 2; i < n; i++)
    {
        if (n % i == 0)
            return 0;
    }
    if (i == n)
        return 1;
}

//1.判断一个数是不是素数
//int main()
//{
//    int n = 0;
//    scanf("%d", &n);
//    int ret = is_prime(n);
//    if (ret == 1)
//        printf("prime number.");
//    else
//        printf("not prime number.");
//
//    return 0;
//}

//2.利用函数打印100~200之间的素数
int main()
{
    int j = 0;
    int ret = 0;
    for (j = 101; j <= 200; j++)
    {
        ret = is_prime(j);
        if (ret == 1)
            printf("%d ", j);
        else
            continue;
    }

    return 0;
}
*/

///作业2：实现函数判断year是不是闰年
/*
#include <stdio.h>

int is_leap_year(int year)
{
    if ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0))
        return 1;
    else
        return 0;
}

int main()
{
    int year = 0;
    scanf("%d", &year);
    if (is_leap_year(year))
        printf("It is leap year.");
    else
        printf("It is not leap year.");

    return 0;
}
*/
