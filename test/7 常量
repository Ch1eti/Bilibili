// 常量
// 1.字面常量    30     3.14    'w'//字符    'abc'
// 2.const修饰的常变量   const int a = 20 后面就不能再改变a  // const修饰的a，本质是变量，有常量的属性
// 3.#define定义的标识符常量  跟全局常变量差不多   #define MAX 100   #define STR 'abcdef'
// 4.枚举常量  三原色 Red Green Blue    enum Color
//


#include <stdio.h>

enum Color{
    //枚举常量
    RED,
    GREEN,
    BLUE
};


int main()
{
    int num = 10;
    enum Color c = RED;
    return 0;
}
