// 常见的关键字
// C语言本身内置的，不能自己创建

// auto  break  case  const  default  continue  di  else  enum  extern  register
// return  sizeof  static  struct  switch  typedef  union  volatile  while
//
// char  short  int  long  float  double  signed  unsigned  void

// 变量的命名
// 1.有意义  2. 名字必须是字母、数字、下划线组成，不能有特殊字符，不能以数字开头  3. 变量的名字不能是关键字






#include <stdio.h>

int main()
{
    int a = 10; // 自动变量

    return 0;
}
