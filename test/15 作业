//
//0 - 数字0
//'0' - 字符0 ASCII值是48
//'\0' - 字符 ASCCII值是0
// EDF - end of file 文件结束的标志 值是-1

#include <stdio.h>
#include <cstring>


//int main()
//{
//    char arr[] = {'b', 'i', 't'};
//    //[b i t][      ?      ]         char arr[4] = {'b', 'i', 't'} 不完全初始化，剩余部分默认初始化为0，与\0没区别
//    printf("%i\n", strlen(arr));
//    return 0;  //答案 随机
//}



// 求两个数的最大值
/*int Max(int x, int y)
{
    if(x>y)
        return x;
    else
        return y;
}

int main()
{
    int a = 0;
    int b = 0;
    scanf("%i %i", &a, &b);
    int r = Max(a,b);
    printf("%i\n", r);

    return 0;
}
*/


// 计算y的值
// 已知y=f(x)，x<0,y=1;x=0,y=0;x>0,y=-1
/*int main()
{
    // 输入
    int x = 0;
    int y = 0;
    scanf("%i", &x);
    // 计算
    if(x<0)
        y = 1;
    else if(x==0)
        y = 0;
    else
        y = -1;
    // 输出
    printf("%i\n", y);
    return 0;
}
*/
